<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <typeAliases>
        <!-- 批量别名定义，扫描整个包下的类，别名为类名（首字母大写或小写都可以） -->
        <package name="com.hand.domain.entity"/>
    </typeAliases>
    <!--—使用自动扫描器时，mapper.xml文件如果和mapper.java接口在一个目录则此处不用定义mappers
    在这儿分别把xml和mapper放在java和resources目录下就相当于放到同一目录，只要包路径一样就行
    因为resource和java下面效果一样，都是classpath-->
    <!--<mappers>-->
        <!--<package name="com.hand.infra.mapper"/>-->
    <!--</mappers>-->
</configuration>

